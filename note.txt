frontend/
├── public/                  # Public assets like favicons or static logos
├── src/
│   ├── assets/              # Images, CSS/SCSS files, fonts (e.g., Himalaya photos)
│   ├── components/          # Reusable UI elements
│   │   ├── Navbar.jsx
│   │   ├── Footer.jsx
│   │   ├── CampaignCard.jsx
│   │   └── PhotoGallery.jsx
│   ├── pages/               # Main route views
│   │   ├── Home.jsx         # Landing page
│   │   ├── Awareness.jsx    # Info on Himalayan cleanliness
│   │   ├── Campaigns.jsx    # Active & past cleanup drives
│   │   ├── Achievements.jsx # Photo galleries and milestones
│   │   ├── Volunteer.jsx    # Registration form
│   │   └── Donate.jsx       # Donation support information/portal
│   ├── services/            # API call logic (e.g., Axios/Fetch setups)
│   │   └── api.js           
│   ├── App.jsx              # Main router configuration
│   └── main.jsx             # React DOM rendering entry point
├── .env                     # Environment variables (e.g., VITE_API_BASE_URL)
├── package.json
└── vite.config.js



backend/
├── config/                  # Configuration files
│   └── db.js                # Database connection setup (e.g., MongoDB/Mongoose)
├── controllers/             # Logic for handling requests and responses
│   ├── volunteerController.js
│   ├── campaignController.js
│   └── donationController.js
├── models/                  # Database schemas
│   ├── Volunteer.js         # Schema for volunteer details
│   ├── Campaign.js          # Schema for campaign data
│   └── Donation.js          # Schema for tracking donations
├── routes/                  # API endpoint definitions
│   ├── volunteerRoutes.js   # e.g., POST /api/volunteers/register
│   ├── campaignRoutes.js    # e.g., GET /api/campaigns
│   └── donationRoutes.js    # e.g., POST /api/donations
├── middlewares/             # Custom middleware functions
│   ├── errorHandler.js      # Global error handling
│   └── corsPolicy.js        # Crucial for local dev and frontend deployment
├── .env                     # Secrets (PORT, DB URI, API Keys)
├── package.json
└── server.js                # Entry point to initialize Express